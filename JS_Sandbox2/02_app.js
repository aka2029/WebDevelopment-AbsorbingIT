/* After this modern JS, jQuery is never suggested as it is unnecessary most of the time, it adds bloat to the application.
-> one things that jQuery is good for is quick simple plugins or scripts that you need to do a specific action but never for DOM manipulation
 */

// DIFFERENT SELECTORS - COUPLE ELEMENT & SINGLE ELEMENT SELECTORS
